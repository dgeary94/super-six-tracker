VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only — bypasses RLS, needed for scripts/migrate-csv-to-supabase.js.
# Never expose this to the client (do not prefix with VITE_).
SUPABASE_SERVICE_ROLE_KEY=
